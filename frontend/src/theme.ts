import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#1976d2', // Cor principal (azul padrão)
        },
        secondary: {
            main: '#ff4081', // Cor secundária
        },
    },
});

export default theme;
